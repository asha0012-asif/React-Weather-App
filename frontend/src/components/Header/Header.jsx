import "./Header.css";

const Header = () => {
    return (
        <header className="header">
            <h1 className="title">React Weather App</h1>
        </header>
    );
};

export default Header;
